import org.junit.Test

class DummyIntergrationTest {

    /**
     * Just testing out the new unbroken-dome plugin settings.
     */
    @Test
    fun `wrote a test for the sake of having a test` () {
        println("You're running an integration test.")
    }
}